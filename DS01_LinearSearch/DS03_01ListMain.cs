using System;
using System.Collections;
using System.Collections.Generic;
namespace DS03_01ListMain
{
    /*
    class MainApp
    {
        static void Main(string[] args)
        {
            List<int> list = new List<int>();
            int data;
            //5개의 데이터 저장
            list.Add(11); list.Add(11);// LInsert(&list, 11);
            list.Add(22); list.Add(22);
            list.Add(33);

            //저장된 데이터의 전체 출력
            Console.WriteLine("현재 데이터의 수: " + list.Count);//LCount(&list));

            //첫 번째 데이터의 조회 c#이라 안함
            Console.WriteLine(list[0]+" ");//LCount(&list);

            // 데이터 조회
           for(int i=1; i<list.Count; i++) {
                Console.Write("{0} ", list[i]);
            }

            //숫자 22를 탐색하여 모두 삭제
            for(int i=list.Count-1; i>=0; i--)
            {
                if(list[i] == 22)
                {
                    list.RemoveAt(i);
                }
            }
            //삭제 후 남은 데이터 전체 출력
            Console.WriteLine("현재 데이터의 수 : {0}", list.Count);
            foreach(Object obj in list)
            {
                Console.Write(obj + " ");
            }
        }

    }*/
}
