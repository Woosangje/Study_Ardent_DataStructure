using System;
using System.Collections;
using System.Collections.Generic;
namespace DS03_01ListMain
{
    /*
    class MainApp
    {

        static void Main(string[] args)
        {
            List<int> list = new List<int>();
            int data;
            list.Add(11); list.Add(11);// LInsert(a,b);
            list.Add(22); list.Add(22);
            list.Add(33);

            Console.WriteLine("현재 데이터의 수: " + list.Count);

            //if(LFirst(a, b);
            Console.Write(list[0]+" ");
            foreach (object obj in list)
            {

                Console.Write(obj+" ");
            }
            //숫자 22를 탐색하여 모두 삭제
            for(int i=0; i<list.Count; i++)
            {
                
                if(list[i] == 22)
                {
                    list.RemoveAt(i);
                    i -= 1;
                }
            }
            //삭제 후 남은 데이터 전체 출력
            Console.WriteLine("현재 데이터의 수 : {0}", list.Count);
            foreach(Object obj in list)
            {
                Console.Write(obj + " ");
            }
        }

    }*/
}
