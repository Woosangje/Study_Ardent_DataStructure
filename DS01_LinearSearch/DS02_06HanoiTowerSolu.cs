using System;
using System.Linq;
using static System.Console;
namespace DS02_06HanoiTowerSolu
{

    /*
 class MainApp
 {
     static void HanoiTowerMove(int num, char from, char by, char to)
     {
         if(num == 1)
         {
             WriteLine("원반1을 {0}에서 {1}로 이동", from, to);
         }
         else
         {
             HanoiTowerMove(num - 1, from, to, by);//2  1
             WriteLine("원반{0}을(를) {1}에서 {2}로 이동", num, from, to);
             HanoiTowerMove(num - 1, by, from, to);//b a c
         }
     }
     static void Main(string[] args)
     {
         //막대A의 원반 3개를 막대B를 경유하여 막대C로 옮기기
         HanoiTowerMove(3, 'A', 'B', 'C');

     } 

 }*/
}
