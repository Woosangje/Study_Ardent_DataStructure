using System;
using static System.Console;
namespace DS01_LinearSearch
{
    /*
    class DS01_LinearSearch
    {
        static public int LSearch(int[] ar, int len, int target)//순차 탐색 알고리즘 적용된 메소드
            {
                int i;
                for(i=0; i<len; i++)
                {
                    if (ar[i] == target)
                        return i;//찾은 대상의 인덱스 값 반환
                }
                return -1;//찾지 못했음을 의미하는 값 반환
            }
        static void Main(string[] args)
        {
            

           
                int[] arr = new int[] { 3, 5, 2, 4, 9 };
                int idx;

                idx = LSearch(arr, arr.Length, 4);
                if (idx == -1)
                    WriteLine("탐색 실패 \n");
                else
                    WriteLine("타겟 저장 인덱스: {0} \n", idx);

                idx = LSearch(arr, arr.Length, 7);
                if (idx == -1)
                    WriteLine("탐색 실패 \n");
                else
                    WriteLine("타겟 저장 인덱스: {0} \n", idx);

                    
            
        }
    }*/
}
